# Session Memory — 2026-09-05

> Root guideline: `.AGENT_DOCS/SYSTEM_GUIDELINE.md`. Session commands: `.AGENT_DOCS/sops/AGENTS_RULE.md`.

## This session

- **Created the `.AGENT_DOCS/` documentation layer** (adapted from the APH-BE pattern, minimized for a static site):
  - `.AGENT_DOCS/SYSTEM_GUIDELINE.md` — root guideline (project overview, repo layout, theme/CSS conventions, routing, hygiene).
  - `.AGENT_DOCS/projectArchitecture/PROJECT_ARCHITECTURE.md` — layout, page list, request flow, CSS load order, theme source relationship, known issues.
  - `.AGENT_DOCS/sops/AGENTS_RULE.md` — session commands (`load`, `keep`, `read`, `save memo`, `status`, `shortcuts`) + context-efficiency rules.
  - `.AGENT_DOCS/memory/2026-09-05.md` — this file.
- **Updated `AGENTS.md`** — added an "Agent docs layer" section pointing agents to `.AGENT_DOCS/` on session start.

- **Rewrote `.AGENT_DOCS/sops/AGENTS_RULE.md`** to mirror the full APH-BE `sops/AGENTS_RULE.md` structure (V1.0): complete command set (`load`, `keep`, `read`, `clear`, `archive`, `status`, `refresh`, `save memo`, `save changes`, `git updates`, `git pull`, `git push`, `create branch`, `shortcuts`), context-management targets, memory classification, development-workflow w/ plan confirmation, reading/search/workflow rules, session hygiene, git operations, start/end-session behavior. Laravel-only bits adapted: no pint/composer (verification = wiring checks), branch names are plain `feat-`/`fix-`/`update-` (no HRDD), folder path resolution is repo-root only.

## Scope decisions

- Deliberately **excluded** the APH-BE-specific subfolders that don't fit this repo: `api/` (no API), `sync/` (no cross-project lookup), `changes/` (no module CRs), and `HRDD.md` (git branching — this repo uses a single `main` branch with plain commit messages).

## Next steps

1. (Empty — session closed cleanly. Future sessions: continue normal content edits to `index.html`, `laptop-svg.html`, `md-reader.html`, `assets/`.)